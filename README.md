# WhatsApp Chat Analyzer Agent


Upload WhatsApp exported `.txt` files, build a vector index, and ask natural language queries over your chat. Includes visualizations and sentiment trend detection.


See `.env.example` for environment variables. Run `streamlit run streamlit_app.py`.
